import type { CardSummaryResponse } from "../../../types/card"; import { CardListItem } from "../CardListItem"; import styles from "./CardList.module.css";
export function CardList({cards,selectedId,onSelect,onEdit,onArchive,onRestore}:{cards:CardSummaryResponse[];selectedId:number|null;onSelect:(id:number)=>void;onEdit:(id:number)=>void;onArchive:(id:number)=>void;onRestore:(id:number)=>void}){return <div className={styles.list} role="table" aria-label="Карточки"><div className={styles.head} role="row"><span>Лицевая сторона</span><span>Обратная сторона</span><span className={styles.optional}>Статус</span><span>Действия</span></div>{cards.map(card=><CardListItem key={card.id} card={card} selected={card.id===selectedId} onSelect={onSelect} onEdit={onEdit} onArchive={onArchive} onRestore={onRestore}/>)}</div>}
