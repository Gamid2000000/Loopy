export const paths = {
  dashboard: "/dashboard",
  decks: "/decks",
  deck: "/decks/:deckId",
  cards: "/decks/:deckId/cards",
  studySession: "/study-sessions/:sessionId",
  statistics: "/statistics",
  profile: "/profile",
} as const;
